# Environment / secrets
.env
.env.*
!.env.example
# Local Streamlit secrets (use Cloud UI or secrets.toml.example only in repo)
.streamlit/secrets.toml

# Python
__pycache__/
*.pyc
*.pyo

# Python venvs and tool caches
.venv/
venv/
env/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Runtime artifacts
.sec_cache.db
.warehouse.db
.fmp_cache.db
.universe_cache.db
.wrds_pit.db
.sentiment_cache.tar.gz
.DS_Store
reports/
.seeded_tickers.txt
backtests/
reproducibility_*.json
backtest_bt_*.json
backtest_wf_*.json
phase0_*.json
.xgb_features_*.csv
# Modal CPCV run summaries (written by scripts/run_modal_cpcv.py)
# also: Phase 1/2 candidate + AI-pick artifacts (regenerable)
runs/

# Trained-model binaries and large training frames (regenerable)
models/*.pkl
models/training_frame.csv

# Large daily-returns dumps from Phase 3 eval (small metrics files are tracked)
docs/eval/three_series/daily_returns.csv

# Superpowers / agent workspaces
.superpowers/
.worktrees/

# Per-provider runtime caches
quant/.kalshi_cache/

# Jupyter notebooks (local-only by default; force-add if you want to track)
notebooks/

# Build artifacts
*.egg-info/

# Frontend
node_modules/
frontend/dist/
frontend/.env.local

# IDE / tooling
.claude/
.vercel
.price_cache/
.fred_cache/
.sentiment_cache/
