# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# LLM
anthropic>=0.39.0
openai>=1.0.0

# Data / finance
pandas>=2.0.0
numpy>=1.24.0
requests>=2.25.0
yfinance>=0.2.54
fredapi>=0.5
edgartools>=2.0
scipy>=1.10

# Quant modeling (required by Modal CPCV workers when signals are enabled)
scikit-learn>=1.3.0
xgboost>=2.0.0
statsmodels>=0.14.0        # ARIMA signal (quant/arima_signal.py)
filterpy>=1.4.5            # Kalman filtering in regime classifier
pykalman>=0.9.7            # alt Kalman implementation used by some signals

# Market data
tavily-python>=0.3.0

# Sentiment analysis
vaderSentiment>=3.3.2

# PDF / reports
fpdf2>=2.8.0

# HTML parsing
beautifulsoup4>=4.12.0
lxml>=5.0.0

# RAG
pinecone>=3.0.0

# Config / env
python-dotenv>=1.0.0
pydantic>=2.0
pydantic-settings>=2.0

# Observability
sentry-sdk[fastapi]>=2.0

# Alpaca paper trading
alpaca-py>=0.28.0

# Scheduling
APScheduler>=3.10.4

# Cache backend (redis client — used by timesfm_cache tests)
redis>=5.0.0

# Cloud compute (Modal backtesting fan-out)
modal>=0.64.0
